# RPi 두뇌 의존성
#
# ① 구동 · ② 속도제어 (DJ)
#   lgpio: 라즈베리파이 5 GPIO/PWM (Pi 5는 RP1 칩 때문에 RPi.GPIO 미작동).
#          Linux(라즈베리파이)에서만 설치. 개발 PC에서는 자동으로 MockGpio 사용.
lgpio; sys_platform == "linux"

# ③ 데이터수집 · 인지 · ④ 앱통신 (CW) — 착수 시 주석 해제
# smbus2                 # MPU-6050 I2C
# bluezero               # BLE GATT 서버
# numpy                  # 윈도우/특징
# tflite-runtime; sys_platform == "linux"   # 노면 추론(Pi)

# 개발/테스트
pytest
