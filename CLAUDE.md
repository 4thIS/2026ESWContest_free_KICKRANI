# CLAUDE.md

이 파일은 Claude Code(claude.ai/code)가 이 저장소에서 작업할 때 참고하는 가이드다.

## 프로젝트 개요

**임베디드 소프트웨어 공모전 - 노면 감지 킥보드** (제24회 임베디드SW경진대회 자유공모, 예선 마감 2026-09-03) — 팀 **킥라니**, 제출 레포 `4thIS/2026ESWContest_free_KICKRANI`

진동(IMU)으로 주행 중 **노면 재질을 실시간 판별**해, 위험한 노면이면 자동 **감속**, 안전한 노면이면 정상 속도로
달리는 **적응형 속도제어 킥보드**. 데이터 수집과 시연을 모두 동일한 RC카(킥보드 모형)로 수행 → **학습=배포 환경 일치(도메인 갭 0)**.

- **노면 5종**: 아스팔트·자전거도로(안전) / 보도블럭·콘크리트(주의) / 자갈길(위험)
- **핵심 논리**: 카메라는 노면이 "어떻게 보이는지"를 보고, 진동은 "실제로 어떤지"를 느낀다 → 저전력·전천후·프라이버시 이점
- **플랫폼**: Raspberry Pi 5가 **구동·속도제어·데이터수집·추론·앱통신을 모두 통합** 담당
  (초기엔 ESP32 별도 수집부 구조였으나 Pi 통합으로 전환)

## 저장소 구조 & 담당

| 폴더 | 내용 | 담당 | 브랜치 |
| --- | --- | --- | --- |
| `RPi/` | 구동·속도제어(**동제**) / 데이터수집·앱통신·인지(**찬우**) | 동제·찬우 | `dj` · `cw` |
| `app/` | 스마트폰 앱(Bluetooth RFCOMM 컨트롤러) | 도현 | `dh` |
| `sash/` | RC카 틀(섀시) 3D 프린팅 파일 (회로·보드 배치는 RPi 담당과 소통) | 명재 | `mj` |
| `docs/` | 전체 프로젝트 문서 + 파트별 문서(RPi_docs / app_docs) | 공통 | — |

## 아키텍처 (Pi 통합, 독립 모듈 → 통합)

4가지 기능을 **각각 독립 서브시스템**으로 개발한 뒤, **계약(인터페이스)** 으로 통합한다.

| # | 서브시스템 | 인터페이스(계약) | 의존 | 담당 |
| --- | --- | --- | --- | --- |
| ① | 구동 | `Motor.set_duty()/stop()` | GPIO | 동제 |
| ② | 속도제어 | `SpeedController.set_target()/update()` | ①+엔코더 | 동제 |
| ③ | 데이터수집 | `Sampler.start()`·`Logger.open/write/close` | 센서 | 찬우 |
| — | 노면인지 | `Windower/Model/policy` | ③의 `Sample` 계약 | 찬우 |
| ④ | 앱통신 | `BleServer.on_command()/send_telemetry()` | RFCOMM socket | 찬우 |
| 통합 | controller | 위를 모드별로 배선(IDLE/COLLECT/DEMO) | 전부 | 찬우 |

> ④ 앱통신은 **Pi측 RFCOMM(SPP) 서버**(찬우) — 앱 완성본 규격에 Pi가 맞춤(issue #10 종결). 스마트폰 앱 자체는 `app/`(도현).

- **공통 계약(먼저 고정)**: `Sample` 스키마, 앱 명령/텔레메트리(RFCOMM·JSON), 노면 클래스. 계약이 고정돼야 각자 병렬 개발 가능.
- **안전 최우선**: 움직이는 차량이므로 STOP/연결끊김/에러/종료 시 **항상 모터 먼저 정지**. 불확실하면 감속/정지.
- 상세: [`docs/RPi_docs/설계명세서.md`](docs/RPi_docs/설계명세서.md)

## 기술 스택

- **RPi**: Python 3 · `smbus2`(I2C) · `gpiozero`+`lgpio`(GPIO/PWM) · `numpy`(RF 추론) · 표준 `socket`(RFCOMM) · `numpy` · `pytest`
- **학습**: PC(오프라인, `RPi/scripts/train.py` sklearn RF) — 세션 단위 train/test 분리, **RF→JSON→numpy 추론**(`pi/infer/forest.py`, Pi에 sklearn/TF 불필요)
- **샘플링**: 200Hz(MPU FIFO로 지터 흡수) — 노면 고주파 정보를 담기 위한 나이퀴스트 근거

---

# 협업 규칙 (⚠️ Claude와 팀원 모두 반드시 준수)

## Git 브랜치 & 머지

1. **각자 자기 브랜치에만 푸시한다.** — `cw`(찬우) · `dh`(도현) · `mj`(명재) · `dj`(동제).
   `main`은 **PR(Pull Request)을 통해서만** 머지한다.
2. **`main`에 직접 push 금지. `--force`(force push) 등 히스토리를 바꾸는 명령은 절대 사용하지 않는다.**
   (`git push --force`, `git push origin main`, `git reset --hard` 후 강제 푸시 등 전면 금지)
3. **머지 권한(admin) 관련**: 모두 admin이라 머지할 권한은 있으나,
   - **본인 작업 영역(자기 폴더) 안의 변경**만 있는 PR → 리뷰 후 머지 가능.
   - **남의 작업 영역(다른 폴더 등)에 변화를 끼치는 PR** → **본인이 머지하지 않는다.**
     해당 담당자에게 **먼저 통지**하고, **상대방의 피드백을 받은 뒤** 머지한다.
   - 공통 자산(**공통 계약: `Sample` 스키마·BLE 프로토콜·CSV 스키마**, `docs/`, 루트 설정)은
     관련 담당자와 **합의 후** 변경한다.

## Claude가 이 저장소에서 지킬 것

- **절대 `main`에 직접 push하거나 force push하지 않는다.** 현재 작업자의 브랜치에서 작업하고 PR로 올린다.
- 변경이 **다른 사람의 폴더·공통 계약에 영향**을 주면, **머지하지 말고 사용자에게 그 사실을 알리고** 담당자 확인을 받도록 안내한다.
- 커밋/푸시/PR은 **사용자가 요청할 때** 수행한다.

## 일반 협업 규칙 (상식)

- **남의 폴더 파일을 함부로 수정하지 않는다.** 필요하면 담당자에게 먼저 이야기한다.
- **자주 pull** 해서 최신 `main`과 동기화한다. 충돌은 강제로 밀지 말고 소통으로 해결한다.
- **작은 단위로 자주 커밋**하고, 커밋 메시지는 무엇을·왜 바꿨는지 명확히 쓴다.
- **PR 설명**에 변경 내용·의도·영향 범위를 적고, 남의 영역에 영향이 있으면 리뷰어로 지정한다.
- **`main`은 항상 동작 가능한 상태**로 유지한다(빌드/실행이 깨진 채 머지하지 않는다).
- **리뷰 요청·응답에 성실히** 임한다. 통지받으면 빠르게 피드백한다.
- **비밀정보(키·토큰·개인정보)를 커밋하지 않는다.** 필요한 값은 설정/환경으로 분리.
- **공통 계약(인터페이스)이 바뀌면** 영향받는 담당자 전원에게 알린다(각 파트가 이 계약에 의존).

## 문서

- 전체·파트별 문서: [`docs/`](docs/) — Pi 통합 설계명세서는 [`docs/RPi_docs/설계명세서.md`](docs/RPi_docs/설계명세서.md).
