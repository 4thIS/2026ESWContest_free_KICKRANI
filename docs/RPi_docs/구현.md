# RPi 구현 계획 (Implementation Plan)

> 제24회 임베디드SW경진대회 자유공모 · 예선 마감 **2026-09-03**
> 설계 근거: [`설계명세서.md`](설계명세서.md) — 이 계획은 그 스펙을 서브시스템 단위로 쪼갠 실행 목록이다.
> 체크(`- [x]`)는 **실제 파일·테스트로 확인된 것만**. 근거: 리포 내 `RPi/`(구동·속도제어 목 검증 완료, `py -m pytest -q` → 75 passed, 2026-07-29 `RPi/pi` 통합).

**목표:** 라즈베리파이 5 한 대가 **① 구동 · ② 속도제어 · ③ 데이터수집 · ④ 앱통신**과 **노면 인지**를 모두 담당하는 적응형 속도제어 킥보드(RC카 모형)를 완성한다. 진동(IMU)으로 노면을 실시간 판별해 위험 노면이면 자동 감속한다.

**아키텍처:** 4개 서브시스템 + 인지를 **각각 독립 모듈**로 개발(계약만 공유) → **통합 계층(controller)** 이 모드별(IDLE/COLLECT/DEMO)로 배선. 단일 프로세스 · 3 스레드(Sampler / Controller / BLE) + 2 큐(`sample_queue`, `command_queue`).

**기술 스택:** Python 3 · `smbus2`(I2C) · `gpiozero`+`lgpio`(GPIO/PWM) · `tflite-runtime`(추론) · `bluezero`(BLE) · `numpy` · `pytest`. 학습은 PC(오프라인), Pi는 추론만.

---

## Global Constraints (전 파트 공통)

- **안전 최우선:** 움직이는 차량. STOP / 연결끊김 / 에러 / 종료 시 **항상 모터 먼저 정지**. 불확실하면 감속/정지. 모터 기본상태 = 정지.
- **독립 개발 → 통합:** 각 서브시스템은 서로의 **계약(인터페이스)만** 알고 내부는 모른다. 하드웨어는 프로토콜/목으로 추상화해 PC에서 단위테스트.
- **TDD:** 실패 테스트 → 최소 구현 → 통과 → 커밋. 작은 단위로 자주 커밋. 접두어 `feat:`/`fix:`/`test:`/`docs:`/`chore:`.
- **개발 OS:** 팀원 Windows. 순수 로직은 Windows에서 `pytest`, 실제 GPIO/I2C/BLE는 Pi 5(SSH)에서 검증.
- **소유자 표기:** 각 태스크에 `[DJ]`(동제·구동/속도) · `[CW]`(찬우·수집/인지/앱통신) · `[공동]`. 남의 폴더·공통 계약 변경은 담당자 합의 후.
- **200Hz 샘플링:** 노면 고주파 정보(자갈길 등)를 담기 위한 나이퀴스트 근거. MPU FIFO로 지터 흡수. DLPF 94Hz 안티에일리어싱.
- **파일 배치:** 리포 내 **`RPi/`** 폴더, 파이썬 패키지는 **`pi`**. 즉 `RPi/pi/` 아래에 `sensors/` `motion/` `hardware/` `collect/` `infer/` `comm/` `controller.py` `main.py` `config.py`. 테스트는 `RPi/tests/`.
  > ℹ️ RPi 코드는 `RPi/pi/` **단일 트리로 통일**(2026-07-29, DJ 파트를 임시 `rc_car/pi/`에서 이동·통합). 이전 `rc_car/` 임시 배치·리포 밖 프로토타입 폴더는 제거됨.
- **⭐ 설계명세서가 유일 기준(single source of truth):** 인터페이스 계약(§2·서브시스템 명세)·핀맵(§8)·상태전이(§5)·데이터규격(§7)은 모두 **설계명세서를 따른다**. 리포 밖 **원본 프로토타입** 코드는 이 계약·핀맵에 **맞춰 수정해** `RPi/pi/`로 이식했다(DJ 파트 완료, 프로토타입 폴더는 이식 후 2026-07-29 제거). 불일치는 예외 없이 **설계명세서 우선**.

---

## 팀 분할 개요

| Phase | 영역 | 담당 | 하드웨어 | 상태 |
|---|---|---|---|---|
| 0 | 환경·하드웨어·리포 셋업 | 공동 | Pi/부품 | 🔶 일부(패키지 골격·Pi5 확정) |
| 1 | 공통 계약 고정 (Sample·BLE·CSV·인터페이스) | 공동 | 아니오 | ✅ 확정(공통계약.md) |
| 2 | ① 구동 (Motor) | **DJ** | 목→Pi | ✅ 이식·정합·검증(목) |
| 3 | ② 속도제어 (SpeedController+엔코더) | **DJ** | 목→Pi | ✅ 이식·정합·검증(목) |
| 4 | ③ 데이터수집 (IMU Sampler + Logger) | **CW** | 목→Pi | ✅ 구현·검증(목) |
| 5 | 노면 인지 (Windower·Model·policy) | **CW** | 아니오 | 🔶 windower·policy·features·stub 검증 / 실 TFLite 대기 |
| 6 | ④ 앱통신 (BLE GATT 서버) | **CW** | 폰 | ✅ 구현·검증(목) / 폰 실기기 대기 |
| 7 | 통합 controller (IDLE/COLLECT/DEMO) | **CW**(공동) | 아니오 | ❌ 미착수(controller.py 없음) |
| 8 | 안전장치·워치독·자원정리 | 공동 | Pi | ❌ |
| 9 | 실주행·검증·대회 제출 | 공동 | Pi 필수 | ❌ |

**병렬 전략:** Phase 1에서 공통 계약(`Sample`/`RoadClass`/`Mode` 자료형, BLE 명령·텔레메트리, CSV 스키마, 파이썬 인터페이스 스텁)을 문서·코드로 고정. 이후 DJ는 2·3, CW는 4·5·6을 **동시** 진행 → Phase 7에서 계약대로 조립.

---

## Phase 0: 환경·하드웨어·리포 셋업 [공동]

### Task 0.1: 리포 구조 스캐폴딩 [공동]

**Files:** Create `RPi/pi/__init__.py`, `RPi/pi/config.py`, `RPi/requirements.txt`, `RPi/pytest.ini`, 서브폴더 `sensors/ motion/ collect/ infer/ comm/`

- [x] **Step 1: `RPi/pi/` 패키지 골격** — `pi/` + DJ `hardware/ motion/ sensors/` · CW `collect/ infer/ comm/` **전부 생성됨**.
- [x] **Step 2: `RPi/requirements.txt`** — `smbus2 gpiozero lgpio bluezero numpy pytest` (CW 의존 활성화, `tflite-runtime`은 추론 단계).
- [x] **Step 3: pytest 설정** — `RPi/pytest.ini`(`testpaths=tests`, `pythonpath=.`). Windows 목 검증 **75 passed**(DJ 35 + CW 40).
- [x] **Step 4: `pi/config.py`** — 핀맵 §8(MPU=GPIO2/3, ENC=GPIO17, L298N 좌 ENA=GPIO18·우 ENB=GPIO13) + PID게인·속도상수 + **③ 샘플링/윈도우/정책 상수**(SAMPLE_RATE_HZ·DLPF_HZ·WINDOW_*·SPEED_*). 방향(IN)은 하드웨어 고정.
  > ℹ️ 4WD·L298N·직진 전용 확정(2026-07-27, §8 갱신). MPU FIFO/DLPF·윈도우·정책 상수 반영 완료.
- [x] **Step 5: 첫 커밋/PR** — PR #3·#5 머지 완료. 이후 트리 통합은 PR #8(CW 리뷰 대기).

> ⚠️ `RPi/`(리포 내)는 Pi 프로그램 공용 폴더다. DJ가 생성한 건 DJ 파트(구동/속도/공유 엔코더)뿐이며, `pi/contracts.py`·`pi/config.py`는 공통이라 CW 확인을 받는다.

### Task 0.2: Pi 5 헤드리스 셋업 (SSH) [공동, Pi 필요]

- [ ] **Step 1: OS 이미징** — Raspberry Pi OS (64-bit), 호스트명·SSH 활성화·시간대 `Asia/Seoul`·Wi-Fi.
- [ ] **Step 2: SSH 접속 확인** — `ssh <user>@<host>.local`.
- [ ] **Step 3: 패키지 설치** — `sudo apt install -y python3-pip i2c-tools`, `pip install -r requirements.txt`.
- [ ] **Step 4: I2C·GPIO 활성화** — `raspi-config`에서 I2C on. `i2cdetect -y 1`로 MPU-6050(0x68) 인식 확인.
- [ ] **Step 5: 권한·시간대 확인** — GPIO 그룹, `timedatectl`로 `Asia/Seoul` 확인.

### Task 0.3: 하드웨어 확보·조립 [공동, ⚠️ 일정 최대 리스크]

> BOM·배선도·조립순서는 설계명세서 §8 배선표 · [`핀맵.html`](핀맵.html) 기준.

- [x] **Pi 모델 확정 — 라즈베리파이 5** (2026-07-20, BOM Pi5 기준 갱신).
- [ ] Pi 5 RAM(4GB 추천) · 벅컨버터 5V/5A 충족 확인
- [x] 속도센서 방식 확정 — **A3144 홀센서+자석** (2026-07-27). DO–VCC 10kΩ 풀업, PPR=자석 수 실측 남음.
- [ ] 모터·배터리·바퀴 규격 확정 → **국내 일괄 발주**
- [ ] ⚠️ **[최우선] 속도별 노면 진동 예비실험** — 조립 전에 자갈/아스팔트를 저·중·고속으로 굴려 IMU 원신호 구분 가능성 확인 (인지 성립 여부 근거)
  > ➕ 도구 준비됨: `RPi/scripts/collect_premise.py`(수집)·`analyze_surfaces.py`(분석)·`pi/infer/features.py`. **실험 실행만 하드웨어 대기.**
- [ ] 섀시 조립 · 속도센서 장착 · 전원부 먼저 5V 실측 (모터/Pi 연결 전)
- [ ] L298N 배선 (+12V·GND·ENA/ENB PWM · IN1~4 +5V/GND 고정) · ⚠️ ENA/ENB 점퍼 제거 · 좌우 방향 일치 확인
- [ ] MPU-6050 I2C 배선 (SDA GPIO2 / SCL GPIO3)
- [ ] ⚠️ **모터·Pi 전원 격리**(별도 배터리/디커플링) — 안 하면 Pi 리셋

---

## Phase 1: 공통 계약 고정 [공동] — **병렬개발 전제**

### Task 1.1: 자료형·모드 계약 [공동] — ✅ 확정([`공통계약.md`](공통계약.md))

**단일 출처:** 계약 원문은 [`공통계약.md`](공통계약.md), 코드는 `RPi/pi/contracts.py`. 여기선 중복 정의하지 않고 참조만 한다.

- [x] **자료형** `Sample`(raw LSB: 가속 ±8g→4096LSB/g, 자이로 ±500dps→65.5LSB/dps, `wheel_pulse`=누적 단조증가) · `RoadClass`(5종) · `Mode`(IDLE/COLLECT/DEMO)
- [x] **인터페이스(Protocol)** `Motor`(①) · `Encoder`(②③ 공유, DJ 소유) · `SpeedController`(②) · `Sampler`/`Logger`(③) · `Windower`/`Model`(인지) · `BleServer`(④) · `policy(road)->float`
- [x] **`RPi/pi/contracts.py`** — 공통계약.md를 코드로 옮김(DJ가 이식). CW 확인 요청.
- [ ] **확정 결정**(공통계약.md): 센서값 raw 정수 · 속도 m/s · BLE JSON · 엔코더 DJ 소유 단일 인스턴스

> DJ 코드는 이 계약에 맞춰 구현 완료(아래 Phase 2·3). 계약 변경은 전원 합의 후 공통계약.md→contracts.py 순으로.

### Task 1.2: BLE 프로토콜 계약 문서 [공동, CW 주도 → 도현 합의]

**Files:** Create `docs/RPi_docs/BLE_프로토콜.md`

> BLE 계약 초안은 [`공통계약.md`](공통계약.md) §계약2에 확정(JSON 문자열, 특성 2개 Command/Telemetry).

- [ ] Service / Characteristic UUID **실제 값** 확정 (현재 `TBD`)
- [x] 명령(앱→Pi) JSON: `{"cmd":"SET_MODE","mode":"collect|demo"}` · `START` · `STOP` · `{"cmd":"SET_LABEL","label":...}`
- [x] 텔레메트리(Pi→앱) JSON: `{state, mode, road, speed, file}` (state∈`IDLE|COLLECT|DEMO|SAFE`, speed=m/s, `conf` 선택)
- [ ] 문서화 후 **도현(app)에게 전달·합의** — 공통 계약이므로 변경 시 전원 통지

> ⚠️ 필드명은 공통계약 확정본 기준 **`road`·`file`**(설계명세서 §2의 `road_class`·`recording_file`에서 단축). `state`(대문자, SAFE 포함)와 `mode`(소문자)가 함께 오는 건 의도된 중복인지 CW 확인.

### Task 1.3: CSV 스키마 계약 [공동, CW]

- [ ] **스키마 고정:** `timestamp_ms, ax, ay, az, gx, gy, gz, wheel_pulse` (설계명세서 §③). 학습 PC 파이프라인과 동일해야 함 → 변경 시 인지 담당과 합의.

> **이 시점부터 DJ는 Phase 2~3, CW는 Phase 4~6 병렬 진행.**

---

## Phase 2: ① 구동 (Motor) [DJ] — ✅ 이식·정합·검증 완료(목)

> **현황:** `RPi/pi/`로 이식·통합 완료, 공통계약 정합, `py -m pytest -q` → **75 passed(전체)**. 실물 검증만 부품 대기.

### Task 2.1: GPIO 백엔드 추상화 [DJ]

**Files:** `RPi/pi/hardware/backend.py`, `tests/test_backend.py`

- [x] **lgpio(실물) ↔ MockGpio 자동 선택 백엔드** — 목 검증 완료
- [x] **이식:** `RPi/pi/hardware/backend.py`로 이식, import 경로(`pi.*`) 정리
- [ ] 설계명세서 §8 PWM 핀(**ENA=GPIO18·ENB=GPIO13**) **하드웨어 PWM 실물 동작 확인** → 안 되면 소프트PWM 폴백 (부품 도착 후)

### Task 2.2: Motor 드라이버 [DJ]

**Files:** `RPi/pi/motion/motor.py`, `tests/test_motor.py`

- [x] **L298N 직진 제어** — 백엔드 주입, 목 검증 (`test_motor.py`)
- [x] **공통계약 정합:** 메서드명 → **`set_duty`** / `stop()`, 핀맵 §8 적용(ENA=18·ENB=13). 방향(IN)은 하드웨어 고정 → 속도 PWM 2선만
- [ ] **실물 검증(부품 후):** 좌·우 같은 방향 회전, `stop()` 즉시 정지 (바퀴 공중)
- [ ] 듀티 상한(`DUTY_MAX`)으로 모터 정격전압 제한 실제값 결정

---

## Phase 3: ② 속도제어 (SpeedController + 엔코더) [DJ] — ✅ 이식·정합·검증 완료(목)

> **현황:** `RPi/pi/`로 이식·통합 완료, 공통계약 정합, **75 passed(전체)**. 실물 튜닝만 부품 대기.

### Task 3.1: 엔코더 드라이버 [DJ]

**Files:** `RPi/pi/sensors/encoder.py`, `tests/test_encoder.py`

- [x] **펄스 카운트 → 속도(m/s)·거리(m) 환산** — 엣지 콜백, 목 검증 (`test_encoder.py`)
- [x] **공통계약 정합:** `pulses()` 메서드 + **`speed_mps()` 무인자**(내부 시계). ENCODER_PIN → **GPIO17**(§8)
- [x] **속도 측정 = 펄스 주기 기반**(마지막 두 펄스 간격) — A3144 자석 4개(저PPR·저속)에서도 0/급등 없이 안정 수렴. 매틱 카운트 방식은 자석 4개면 목표의 ~1.8배로 폭주 → 폐기. 정지 판정 `ENCODER_STOP_TIMEOUT_S`.
- [x] **엔코더 공유(공통계약 확정):** DJ 소유 **단일 인스턴스**를 ②·③이 주입받아 공유. `SpeedController.stop()`은 공유 누적펄스를 **리셋하지 않음**(단조증가 보장)
- [ ] `ENCODER_PULSES_PER_REV`·`WHEEL_CIRCUMFERENCE_M` **실측 보정** (A3144 자석 4개→PPR=4 / 지름65mm→0.204. 자석 수·바퀴 실측 시 이 둘만 조정)
- [ ] 엔코더 캘리브레이션 스크립트 (바퀴 N회전 → 펄스 카운트)

### Task 3.2: PID + SpeedController [DJ]

**Files:** `RPi/pi/motion/speed_controller.py`, `RPi/pi/motion/pid.py`, `tests/test_pid.py` `tests/test_speed_controller.py`

- [x] **anti-windup PID** — `RPi/pi/motion/pid.py` (목 검증)
- [x] **폐루프 정속 주행** — `RPi/pi/motion/speed_controller.py`, 목 주행 물리(`sim.py`)로 목표속도 수렴 확인
- [x] **공통계약 정합:** `Driver`(IDLE/DRIVING) → **`SpeedController`**(`set_target(speed_mps)`/`update()`(무인자, 내부 시계)/`stop()`). 모드 상태머신은 제거 → Phase 7 controller로 분리
- [ ] **실물 튜닝(부품 후):** `PID_KP/KI/KD` 실물 게인 튜닝, 목표속도 수렴 확인 (엔코더 로그)
- [ ] 소프트스타트 램프 (정지→목표속도 즉시 지령 금지, 기어 보호)

> **참고:** `SpeedController`는 순수 속도 조절기다. 통합 계층의 모드 상태머신(IDLE/COLLECT/DEMO)은 Phase 7의 `controller.py`가 별도로 담당 — 혼동 금지.

---

## Phase 4: ③ 데이터 수집 (IMU Sampler + Logger) [CW] — ✅ 구현·검증(목) / 실물(Pi) 대기

### Task 4.1: MPU-6050 드라이버 [CW]

**Files:** `RPi/pi/sensors/imu.py`, `tests/test_imu.py`

- [x] **Step 1: 실패 테스트** — 목 I2C(FakeBus)로 WHO_AM_I·레지스터 초기화·raw 읽기 검증
- [x] **Step 2: 구현** — `Mpu6050`: WHO_AM_I 확인, DLPF 94Hz, ±8g/±500dps, **FIFO 활성화**, 샘플레이트 200Hz(SMPLRT_DIV)
- [x] **Step 3: FIFO drain** — `fifo_count`/`drain`(12바이트 프레임 파싱)·`overflowed`(오버플로 감지)·`reset_fifo`
- [ ] **단독 테스트(Pi):** 정지 시 `az≈1g`, 축 방향 부호 확인

### Task 4.2: Sampler 스레드 [CW]

**Files:** `RPi/pi/sensors/sampler.py`, `tests/test_sampler.py`

- [x] **Step 1: 실패 테스트** — 목 IMU/엔코더 주입, `drain_once`가 `Sample` 스키마대로 push, 타임스탬프 규칙적(5ms)·단조증가, `wheel_pulse` 스냅샷
- [x] **Step 2: 구현** — 스레드에서 IMU FIFO drain + 엔코더 `pulses()` 스냅샷 → `Sample` 생산 → 큐. 타임스탬프 = 누적인덱스×주기. `start()/stop()` 깨끗한 종료
- [ ] **단독 테스트(Pi):** **실측 200Hz**, 큐 생산율·gap 없음

### Task 4.3: Logger (CSV) [CW]

**Files:** `RPi/pi/collect/logger.py`, `tests/test_logger.py`

- [x] **Step 1: 실패 테스트** — `open(label)`→`write(s)×N`→`close()`, CSV 헤더·스키마 순서(`timestamp_ms,ax..wheel_pulse`)·빈 라벨 fallback 검증
- [x] **Step 2: 구현** — `CsvLogger`: 1주행=1파일 `run_<label>_<ts>.csv`, write + **주기적 fsync**(`LOG_SYNC_SEC`), `close`시 flush
- [ ] **단독 테스트(Pi):** CSV 행수 ≈ 시간×200, gap 없음
- [ ] 디스크 풀/쓰기실패 시 녹화중단·파일닫기·보고 (설계명세서 §6) — 미구현

---

## Phase 5: 노면 인지 (Windower · Model · policy) [CW] — 🔶 일부(windower·policy·features·stub 검증 / 실 TFLite 대기)

> 학습은 PC(오프라인), Pi는 추론만(TFLite). ③의 CSV로 오프라인 학습·검증.

### Task 5.1: Windower [CW]

**Files:** `RPi/pi/infer/windower.py`, `tests/test_windower.py`

- [x] **Step 1: 실패 테스트** — mock `Sample` 스트림 add → 경계에서 `Window` 반환·미달 None·오버랩(hop) 검증
- [x] **Step 2: 구현** — 1차 시간 윈도우(`WINDOW_SAMPLES`)+`hop` 슬라이드(50% 오버랩)
- [ ] 시간 vs 거리 윈도우 비교 실험 (보고서 어필 포인트, 설계명세서 §7)

> ➕ **특징추출** `RPi/pi/infer/features.py`(`tests/test_features.py`) — az(중력제거) RMS·분산·ptp·zcr·dominant freq·대역에너지. 분석·학습 파이프라인 공용. ✅ 검증.

### Task 5.2: Model (TFLite 추론) [CW]

**Files:** `RPi/pi/infer/model.py`, `tests/test_model.py`

- [x] **목 추론기**(`StubModel`, 고정 클래스 반환) → 통합·정책 개발 가능하게 자리 확보 ✅
- [ ] TFLite 로드·`predict(window) -> RoadClass`. 모델 파일 없으면 DEMO 비활성(COLLECT은 가능)
- [ ] 추론 예외/저신뢰(confidence↓) → 윈도우 스킵 + 기본 감속 (안전)
- [ ] **[PC 파이프라인]** 세션단위 train/test 분리, 혼동행렬, TFLite 변환 (별도 학습 저장소/스크립트)
- [ ] **ML 담당자 지정** (역할표 공백 — 착수 전 필수)

### Task 5.3: policy [CW]

**Files:** `RPi/pi/policy.py`, `tests/test_policy.py`

- [x] **Step 1: 실패 테스트** — 노면별 목표속도 매핑: 안전=정상, 주의=소폭제한, 위험=감속, **미지/불확실=fail-safe 감속**
- [x] **Step 2: 구현** — `policy(road) -> float`(SAFE/CAUTION/DANGER 상수 매핑)

---

## Phase 6: ④ 앱통신 (BLE GATT 서버) [CW] — ✅ 구현·검증(목) / 폰 실기기 대기

**Files:** `RPi/pi/comm/protocol.py`·`ble_server.py`, `tests/test_ble_protocol.py`·`test_ble_server.py`

- [x] **Step 0: 프로토콜** — `parse_command`(START/STOP/SET_MODE/SET_LABEL, 불량·미지 None)·`encode_telemetry`(JSON). 순수 로직 검증(8케이스)
- [x] **Step 1: 실패 테스트** — Fake transport로 명령 write → `on_command` 콜백에 파싱 dict 전달, 불량 무시, `send_telemetry` notify 직렬화, `start` 광고
- [x] **Step 2: 구현** — `BleServer`(transport 주입): `on_command(cb)`·`send_telemetry(data)`·`start()`. 실물 `bluezero` transport는 Pi에서 연결
- [ ] **단독 테스트(폰):** 폰 연결→명령 로그, 더미 텔레메트리 notify 표시
- [ ] BLE 끊김 이벤트 → 콜백으로 통지 (통합에서 즉시 stop 연결) — 미구현
- [ ] Service/Characteristic **UUID 실제 값** (현재 `TBD`, Phase 1.2)

---

## Phase 7: 통합 계층 (controller) [CW 주도, 공동] — ❌ 미착수

> 위 5개를 **모드에 따라 배선**하는 얇은 오케스트레이터. 각 서브시스템 인터페이스만 호출(내부 모름), 의존성 주입으로 조립.

### Task 7.1: Controller 상태머신 [CW]

**Files:** `RPi/pi/controller.py`, `tests/test_controller.py`

- [ ] **Step 1: 실패 테스트(순수 로직, 목 주입)** — 상태 전이표(설계명세서 §5) 검증:
  - `IDLE + START(collect)` → COLLECT (Logger.open, ②순항)
  - `IDLE + START(demo)` → DEMO (②순항)
  - `COLLECT + STOP` → IDLE (Logger.close, ①stop)
  - `DEMO + STOP` → IDLE (①stop)
  - `COLLECT/DEMO + BLE끊김/에러` → **즉시 ①stop** + (수집이면 파일보존) → IDLE(SAFE)
- [ ] **Step 2: 구현** — 주입: Motor·SpeedController·Sampler·Logger·인지(Windower/Model/policy)·BleServer. 배선:
  - COLLECT: `sample_queue → Logger` ; ②순항 → ①
  - DEMO: `sample_queue → Windower→Model→policy → ②.set_target → ①` ; `BleServer.send_telemetry`
  - ④ 명령(SET_MODE/START/STOP/SET_LABEL) → 상태 전이
- [ ] **단독 테스트:** 모드 전환 로직 전부 목으로 통과

### Task 7.2: main.py 진입점 [공동]

**Files:** `RPi/pi/main.py`

- [ ] **시작 시퀀스** (설계명세서 §5): config → ③센서 init → ①모터 init(**정지 확정**) → 인지 모델 로드 → ③Sampler 스레드 → ④광고 → **IDLE**
- [ ] **동시성:** Sampler 스레드 / Controller 스레드 / BLE 스레드 + `sample_queue`·`command_queue`·`telemetry(lock)` 배선
- [ ] `RPi/pi/main.py`의 `--real`/목 실행·주기유지 패턴 참고해 통합

---

## Phase 8: 안전장치·워치독·자원정리 [공동] — ❌ 미착수

> 실물 굴리기 전 필수. 현재 안전장치 0개(`RPi/pi/main.py` `run_real`의 `finally`만 존재).

- [ ] **워치독(필수):** Controller 주기 kick, Sampler 사망/정지 >500ms → 워치독이 ①stop
- [ ] **BLE 끊김 워치독** → 자동 정지 + 광고 재개
- [ ] **신호/종료 처리:** SIGTERM/SIGINT·atexit → ①stop → 파일 close → GPIO 정리 → ④정지
- [ ] **데이터 무결성:** 버퍼링 write + 1초 fsync, 1주행=1파일, 비정상 종료 flush·close (Phase 4.3과 연동)
- [ ] IMU 읽기 에러(I2C 예외) → 재시도 → SAFE(①stop)
- [ ] 엔코더 무펄스(일정시간 0) → 속도0 취급·경고
- [ ] 제어 예외 → 듀티0·GPIO LOW·SAFE
- [ ] 최대 주행시간 리밋 / 폭주 방지

---

## Phase 9: 실주행·검증·대회 제출 [공동] — ❌ 미착수

### Task 9.1: 통합 검증 (바퀴 공중 → 실주행)

- [ ] 모드 전환 E2E: 앱[출발]→정속주행→[정지](COLLECT) / 노면별 감속(DEMO)
- [ ] COLLECT gap 없음 · DEMO 반응 <1초 (자갈길 입력 시 1초 내 감속)
- [ ] **안전 시나리오:** BLE끊김→정지 / Sampler사망→워치독정지 / SIGTERM→정지·파일닫힘 / 저신뢰→감속
- [ ] 찬우 ESP(있다면)·도현 앱과 실연동 확인
- [ ] 실주행: 5종 스펙트로그램 구분 / 세션단위 정확도·혼동행렬 / 엔드투엔드(자갈길 감속·복귀, 로그+영상)

### Task 9.2: 데이터·ML 운영

- [ ] 데이터 수집 물량 계획 (클래스당 세션 수·장소·배터리 교체) 숫자 확정
- [ ] 시연 트랙 제작 (아스팔트·자갈·보도블럭 노면 샘플판)
- [ ] TFLite 모델 학습·검증 후 목 추론기 → 실모델 교체

### Task 9.3: 대회 제출

- [ ] 팀명 확정 (20Byte 이내, 영문 대문자)
- [ ] GitHub 레포명 `2026ESWContest_free_<팀명>` (Public 유지)
- [ ] 시연영상 (3분, 720p) · 개발완료보고서 PPT
- [ ] **제출 — 2026-09-03 마감**

---

## 결정 대기 항목 (설계명세서 §12)

| 항목 | 현재 | 필요한 결정 |
|---|---|---|
| ~~Pi 모델~~ | ✅ Pi 5 확정 | RAM(4GB/2GB)만 |
| ~~속도센서~~ | ✅ A3144 홀센서 확정(2026-07-27) | PPR=자석 수 실측만 |
| ~~엔코더 신호 공유~~ | ✅ DJ 소유 단일 인스턴스(공통계약) | 완료 |
| 윈도우 방식 | 미정 | 시간 vs 거리 — 학습 실험으로 |
| ② 제어 방식 | PID 폐루프 구현됨 | 순항속도 수치·노면별 목표값 확정 |
| BLE GATT 스펙 | JSON 계약 확정 / UUID `TBD` | UUID 실제 값 → 도현 합의 |
| BLE state/mode 중복 | telemetry에 `state`+`mode` 병존 | 의도 확인(CW) |
| IMU 배치 | 미정 | 수집부 구성·장착 위치 |
| 팀명/레포명 | 미정 | 발주·제출 전 확정 |
| ~~§8 배선 채널 수~~ | ✅ **4WD 확정**(2026-07-27) · §8 갱신 완료 | 전원 담당(찬우) 통지·전류 재계산 남음 |

---

### 참고: 코드 이식 매핑 (원본 프로토타입 → 리포 내 RPi/pi) — ✅ 완료(DJ 파트)

> 공통계약(§2 계약)·설계명세서(§8 핀맵)에 맞춰 수정 후 이식. `RPi/`에서 `py -m pytest -q` → **75 passed**(2026-07-29 통합 후).
> 원본 = 리포 밖 프로토타입 `../rc_car/src`(이식 후 제거됨) · 결과물 = 리포 내 `RPi/pi`(패키지 `pi`).

| 원본 `../rc_car/src` | 결과물 `RPi/pi` | 서브시스템 | 정합 결과 |
|---|---|---|---|
| `src/config.py` | `pi/config.py` | 공통 | ✅ 핀 §8(A채널·ENC·MPU), B채널 4WD 확장, 상수 이식 |
| `src/hardware/backend.py` | `pi/hardware/backend.py` | ① | ✅ 그대로 |
| `src/hardware/motor.py` | `pi/motion/motor.py` | ① | ✅ `set_speed`→**`set_duty`**, L298N(ENA/ENB) 직진 전용 |
| `src/hardware/encoder.py` | `pi/sensors/encoder.py` | ②③ | ✅ `pulses()` 메서드·**`speed_mps()` 무인자(내부 시계)**·ENC=17·공유 |
| `src/hardware/sim.py` | `pi/hardware/sim.py` | ② | ✅ 목 주행 물리(테스트/데모용) |
| `src/control/pid.py` | `pi/motion/pid.py` | ② | ✅ 그대로 |
| `src/control/driver.py` | `pi/motion/speed_controller.py` | ② | ✅ `tick`→**`update()`**·`set_target_speed`→**`set_target`**·모드 제거(controller로) |
| `src/main.py` | `pi/main.py` | 통합 | ✅ SpeedController 기반 데모(정속). 3스레드·2큐(§4)는 Phase 7 |
| (신규) | `pi/contracts.py` | 공통 | ✅ 공통계약.md 코드화(CW 확인 요청) |

**참고:** 프로토타입 `rc_car/` 폴더(src·docs)는 이식 완료 후 제거됨(2026-07-29). 실물 튜닝값(`ENCODER_*`, `PID_*`)은 부품 도착 후 `config.py`에서 보정.
