# 공통 계약 (Phase 1) — Sample · 인터페이스 · BLE · CSV

> 병렬 개발의 전제. 각 서브시스템은 **이 계약만** 알고 내부는 모른다.
> 근거: [`설계명세서.md`](설계명세서.md) · 실행계획: [`구현.md`](구현.md)
> ⚠️ **공통 자산** — 변경 시 관련 담당자 전원(DJ·CW·도현) 합의·통지 후에만.

## 확정된 결정 (2026-07-21)

| 항목 | 결정 | 이유 |
| --- | --- | --- |
| 센서 값 형식 | **raw 정수(LSB)** | 작고 정밀, PC/모델이 스케일. az≈4096(1g) |
| 속도 단위 | **m/s** | DJ PID 폐루프(`RPi/pi`)가 이미 m/s 기반 |
| BLE 인코딩 | **JSON 문자열** | 단순·유연·디버깅 쉬움, 앱 개발 편함 |
| 엔코더 드라이버 | **DJ 소유, 단일 인스턴스 주입** | ②속도제어·③수집이 같은 Encoder 공유 |

---

## 계약 1 — 자료형 · 인터페이스 (`RPi/pi/contracts.py`)

```python
from typing import TypedDict, Literal, Protocol, Callable

# ── 자료형 ──
class Sample(TypedDict):
    t_ms: int                       # millis 타임스탬프
    ax: int; ay: int; az: int       # 가속도 raw(LSB), ±8g → 4096 LSB/g
    gx: int; gy: int; gz: int       # 자이로 raw(LSB), ±500dps → 65.5 LSB/dps
    wheel_pulse: int                # 엔코더 누적 펄스(단조증가 스냅샷)

RoadClass = Literal["asphalt", "bike_path", "sidewalk_block", "concrete", "gravel"]
Mode = Literal["IDLE", "COLLECT", "DEMO"]

# ── 서브시스템 인터페이스(Protocol) — 각 파트가 구현, 통합이 소비 ──
class Motor(Protocol):              # ① (DJ)
    def set_duty(self, duty: float) -> None: ...   # 0.0~1.0 직진
    def stop(self) -> None: ...

class Encoder(Protocol):            # ②③ 공유 (DJ 소유, 단일 인스턴스 주입)
    def pulses(self) -> int: ...                   # 누적 펄스
    def speed_mps(self) -> float: ...              # 현재 속도 m/s

class SpeedController(Protocol):    # ② (DJ)
    def set_target(self, speed_mps: float) -> None: ...
    def update(self) -> None: ...                  # 주기 호출: 엔코더→Motor 보정
    def stop(self) -> None: ...

class Sampler(Protocol):            # ③ (CW)
    def start(self) -> None: ...                   # FIFO drain+엔코더 → sample_queue
    def stop(self) -> None: ...

class Logger(Protocol):             # ③ (CW)
    def open(self, label: str) -> None: ...
    def write(self, s: Sample) -> None: ...
    def close(self) -> None: ...

class Windower(Protocol):           # 인지 (CW)
    def add(self, s: Sample): ...                  # -> Window | None

class Model(Protocol):              # 인지 (CW)
    def predict(self, window) -> RoadClass: ...

class BleServer(Protocol):          # ④ (CW)
    def on_command(self, cb: Callable[[dict], None]) -> None: ...
    def send_telemetry(self, data: dict) -> None: ...
    def start(self) -> None: ...

# policy(road: RoadClass) -> float   # 목표 속도(m/s)
```

> DJ 초안 대비 추가: **`Encoder` 프로토콜**(공유 명시) · **`Windower`/`Model`**(인지) · 단위 주석.

---

## 계약 2 — BLE 프로토콜

**특성(Characteristic) 2개** (JSON 문자열 UTF-8):
- `Command` — Write (앱 → Pi)
- `Telemetry` — Notify (Pi → 앱)

### 명령 (앱 → Pi)
```json
{"cmd": "SET_MODE", "mode": "collect"}   // mode: collect | demo (IDLE에서만)
{"cmd": "START"}
{"cmd": "STOP"}
{"cmd": "SET_LABEL", "label": "gravel"}  // 수집 파일명 라벨
```

### 텔레메트리 (Pi → 앱)
```json
{"state": "DEMO", "mode": "demo", "road": "gravel", "speed": 0.8, "file": "run_gravel_20260721_143012.csv"}
```
- `state`: `IDLE | COLLECT | DEMO | SAFE`
- `speed`: m/s · `road`: RoadClass · `file`: 현재 녹화 파일(없으면 `null`)
- `conf`(선택): 추론 신뢰도 0~1

### UUID (확정 예정 — 도현과 합의)
| 용도 | UUID |
| --- | --- |
| Service | `TBD-도현합의` |
| Command (Write) | `TBD` |
| Telemetry (Notify) | `TBD` |

---

## 계약 3 — CSV 스키마 (수집)

- **헤더**: `timestamp_ms,ax,ay,az,gx,gy,gz,wheel_pulse`
- 값: **raw 정수**(LSB), `wheel_pulse`는 누적(단조증가)
- **1주행 = 1파일**, 파일명 `run_<label>_<YYYYMMDD_HHMMSS>.csv` (라벨 없으면 `unlabeled`)
- 학습(PC) 파이프라인과 **동일 스키마** 유지 — 변경 시 인지 담당과 합의

---

## 잔여 확정 항목 (합의 필요)

| # | 항목 | 확정 주체 |
| --- | --- | --- |
| 1 | **엔코더 메서드명** (`pulses()`/`speed_mps()` ↔ `RPi/pi/sensors/encoder.py` 실제 명칭) | DJ |
| 2 | **BLE UUID 실제 값** (Service·Command·Telemetry) | CW ↔ 도현 |
| 3 | 텔레메트리 추가 필드(`conf`·배터리 등) 필요 여부 | 도현(앱 UI) |
| 4 | `Sampler`가 매 배치 `Encoder.pulses()` 스냅샷을 각 Sample에 부착 | CW ↔ DJ |

## 변경 규칙

이 문서의 계약은 **모든 파트가 의존**한다. 수정하려면:
1. 영향받는 담당자(DJ·CW·도현)에게 **먼저 통지**
2. 합의 후 이 문서 갱신 → 그다음 `contracts.py` 등 코드 반영
