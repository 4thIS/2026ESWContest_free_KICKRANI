# 전체 실행 계획 — 남은 작업·분담·일정

> 기준일 **2026-08-24**(최초 08-10) · 예선 마감 **2026-09-03** (남은 기간 약 3.5주)
> 이 문서대로 실행하면 제출까지 완료되도록 태스크를 전부 나열한다.
> 상태·설계 근거: [`RPi_docs/설계명세서.md`](RPi_docs/설계명세서.md) · [`RPi_docs/구현.md`](RPi_docs/구현.md) · [`RPi_docs/DJ_협의사항.md`](RPi_docs/DJ_협의사항.md) · issue #10
>
> 📄 **이 `.md`가 정본이다.** [`실행계획.html`](실행계획.html)은 같은 내용의 보기 좋은 뷰이며, **수정은 항상 양쪽 같이** 한다.

## 0. 현재 상태 스냅샷 (2026-08-24 갱신)

| 영역 | 상태 |
| --- | --- |
| RPi 코드 | `RPi/pi/` 단일 트리, **128 tests GREEN**. ①~④·인지·⑦통합·⑧안전장치 전부 목 검증 + **Pi에서 실행 검증** |
| **하드웨어** | ✅ **배선 완료** — MPU-6050(중력 −0.95g 감지) · L298N+모터 4개(**바퀴 회전·정지 실측 확인**) · Pi 전원 분리(`throttled=0x0`). 🔧 **섀시에 바퀴·모터 조립 중** → 배터리만 연결하면 주행 가능 |
| Pi 셋업 | ✅ SSH 키(`ssh kick`) · I2C · 패키지(bluezero 포함) · `setup_check.py` 소프트웨어 항목 전부 PASS |
| 앱 | 도현 Android 앱(RFCOMM) main 머지됨 — **Pi와 규격 불일치(issue #10) 미해결** |
| 안전 | ✅ 워치독·SIGTERM 정지·`estop.py` 비상정지 — **실기 사고(모터 미정지) 겪고 보강, Pi 검증 완료** |
| 문서 | 설계명세서·공통계약·전원계산·핀맵(9단계 배선순서+결선그림)·L298N배선·배선표 최신 |
| 미착수 | 홀센서 연결 · 지면 주행 · 데이터·모델 · 앱 연동 · 제출물 |

**완료 태스크**: A3(배선) · A4 · A5 · A6 · B1 · B2 · B5(일부: MPU·모터) → 상세는 각 항목의 ✅

**🔴 지금 막고 있는 것(블로커)**
1. **A1 예비실험** — 크리티컬 패스 첫 관문. 이제 **RC카 자체로 굴려 수집 가능**(폰 불필요)
2. ~~A2 issue #10~~ ✅ 합의 → ~~B3 RFCOMM 서버~~ ✅ **구현 완료(2026-08-24, 149 tests)** — 남은 건 B4(앱 자전거도로 추가)·B9(실연동)
3. **B7 전 필수 코드 3종** — `DUTY_MAX=0.75`·소프트스타트·스톨감지 **미구현** → 지면 주행 금지(전원계산 §3)

**⚡ 조립 끝나면 바로 할 순서**: 홀센서 연결(B5 나머지) → `setup_check.py --pwm --jitter` → B6 엔코더 보정 → **B7 전 코드 3종** → B7 PID(바퀴 공중→지면) → **A1 예비실험**

---

## 2. 태스크 전체 목록

표기: `[담당]` · **의존** = 선행 태스크 · ⭐ = 크리티컬 패스

### Phase A — 블로커 해소 (W1: 8/10~8/17)

- [ ] **A1** ⭐ 노면 진동 예비실험(F-1) `[CW]` — 폰(phyphox) 또는 Pi+MPU로 5종 노면 × 3회 수집 → `analyze_surfaces.py` 판정. **성립 확인이 전체 전제**
- [x] **A2** ⭐ issue #10 합의 `[CW+DH]` — ✅ **완료 2026-08-14** — **앱 완성본 기준으로 Pi가 맞춤**. 결정: ① 파일관리(LIST_FILES/RENAME/MEMO) Pi 지원 ② 라벨링=수집 후 RENAME(SET_LABEL 폐기) ③ 노면에 **자전거도로 추가**(앱 목록 갱신 필요) ④ 단위 환산은 앱(Pi는 m/s 원값). 자동 확정: RFCOMM·`type` 필드·대문자 SET_MODE·ACK/ERROR → [`공통계약.md` 계약 2](RPi_docs/공통계약.md)
- [x] **A4** ⭐ C-1 전원 재계산 `[DJ→CW 통지]` — ✅ **완료 2026-08-10** → [`RPi_docs/전원계산.md`](RPi_docs/전원계산.md) · **CW 확인 대기**
  - 결과: 모터 **TT 1:48** · 배터리 **2S 18650**(컷오프 **7.0V**) · Pi는 **USB PD 보조배터리 별도**(⚠️ **벅컨버터 발주 불필요**)
  - ⚠️ **L298N 조건부 유지** — 스톨 시 채널당 2.4A로 연속정격(2A) 초과. `DUTY_MAX=0.75`·소프트스타트·스톨감지·방열판 **4개가 전제**
- [x] **A3** ⭐ 하드웨어 배선 `[DJ+CW]` — ✅ **배선 완료 2026-08-14** (MPU·L298N·모터·전원 실측 확인). 🔧 **섀시 조립 진행 중**(바퀴·모터 장착) → 완료 시 주행 가능. ⬜ 남음: **홀센서(A3144) 연결**·1000µF 콘덴서·방열판. 원 계획: 전원부 먼저 5V 실측 → 배선(핀맵.html) → **모터·Pi 전원 격리**
  - 📋 **발주 확정 사양 12품목** → [`RPi_docs/전원계산.md` §7](RPi_docs/전원계산.md) (L298N은 보유품, **벅컨버터 제외**)
  - ⚠️ **전원 격리 ≠ 접지 분리** — 전원 레일만 나누고 **GND는 반드시 공통**. 안 묶으면 PWM·홀센서가 동작 안 함
- [x] **A5** Pi 5 헤드리스 셋업 `[DJ→CW 실행]` — ✅ **완료 2026-08-14**. SSH 키 인증(`ssh kick`/`kick-ip`) · I2C 활성 · 패키지(smbus2·lgpio·numpy·pytest, ~~bluezero~~ 폐기) · `setup_check.py` 소프트웨어 항목 **전부 PASS** · **Pi에서 128 tests GREEN**. ⬜ `--pwm`/`--jitter` 실측은 B7 전에
  > ➕ **검증 도구 준비됨**: `RPi/scripts/setup_check.py` — 플랫폼·패키지·I2C/MPU·GPIO권한을 PASS/FAIL로 일괄 확인(기본 실행은 **모터 안 돎**). 셋업 절차는 [`RPi/README.md`](../RPi/README.md#pi-5-셋업-실행계획-a5). **실행만 Pi 대기.**
  - **B7 전 필수**: `--pwm`(ENA/ENB 실출력 ⚠️바퀴 공중) · `--jitter`(루프백으로 주기·듀티 실측, ENA→GPIO27 점퍼)
  - ⚠️ config·§8은 GPIO18/13을 "HW PWM"이라 적었으나 실제 백엔드는 `lgpio.tx_pwm`(`pi/hardware/backend.py:90`). 지터가 크면 ②PID가 진동하고, 그 진동이 IMU에 실려 **③ 수집 데이터를 오염**시킴
- [x] **A6** ⑦ 통합 controller `[CW]` — ✅ **완료 2026-08-10** (PR #15) → `RPi/pi/controller.py` (21 tests)
  - 상태머신(§5 전이표) + 모드별 배선(COLLECT→Logger / DEMO→Windower→Model→policy→②) + 안전 정지 경로 일원화(`_safe_stop`: **모터 먼저→파일→IDLE**)
  - 추론 실패 → `SPEED_DANGER_MPS` fail-safe 감속 / 모드 미선택 START·불량 명령 무시
- [ ] **A7** 3D 섀시·부품 마운트 설계 `[MJ]` — IMU는 차체 리지드 마운트, 배터리 바닥, 커넥터 접근구. 회로 배치는 DJ와 소통

### Phase B — 통합·실측 (W2: 8/17~8/24)

- [x] **B1** ⭐ 통합 `main.py` `[CW]` — ✅ **완료 2026-08-10** (PR #15) → `RPi/pi/app.py` 신규 + `main.py` 확장 (8 tests)
  - 3스레드(Sampler/Controller/BLE)+2큐 배선, 50Hz 제어·5Hz 텔레메트리, `stop()` 멱등
  - **엔코더 단일 인스턴스를 ②·③에 공유 주입**(공통계약 B-1) — 테스트로 강제
  - 시작 시퀀스: GPIO → **모터 정지 확정** → 엔코더/PID/속도제어 → IMU → Sampler → BLE → IDLE
  - `--app` / `--app --real` 추가, **DJ 속도데모 보존**(회귀 확인). `make_ble()` → B3에서 `RfcommServer` 연결 ✅
- [x] **B2** ⑧ 안전장치 `[CW]` — ✅ **완료 2026-08-10** (PR #15) → `RPi/pi/safety.py` 신규 (9 tests)
  - **워치독**: 제어 루프가 50Hz `kick()`, **별도 스레드**가 감시(루프가 죽어도 감시 계속) → 500ms 무응답 시 모터 정지. 실동작 검증: 제어 스레드 강제 종료 → **0.8초 내 DEMO→IDLE 자동 정지**
  - **SIGTERM/atexit 모터정지** ✅ B7보다 먼저 선반영 완료(`install_safety_handlers`) — `try/finally`는 SIGTERM·SSH 끊김에서 안 탐
  - **앱 끊김 훅**: `RfcommServer.on_disconnect` 자동 등록 → 즉시 정지 ✅(B3에서 실연결)
  - ⬜ 남음: IMU I2C 재시도→SAFE (실 하드웨어 단계)
- [x] **B3** ⭐ 통신 실구현 `[CW]` — ✅ **완료 2026-08-24** → `pi/comm/{protocol,files,rfcomm_server}.py` (31 tests, 전체 149). RFCOMM(SPP) 서버(표준 socket) + 앱 규격 프로토콜(`type`·ACK/ERROR·대문자 모드→내부 소문자) + 파일관리(LIST_FILES/RENAME/MEMO, 메모 사이드카) + STATUS(speed·distance·vibration·roadType 한글). `controller.handle_command`가 bool 반환(ACK/ERROR 근거), `SET_LABEL` 폐기, `ble_server.py`·bluezero 삭제. `scripts/rfcomm_setup.sh`(SDP 등록). ⬜ 남음: Pi 실기 bind 확인(Pi 오프라인) → B9
- [ ] **B4** 앱 수정 `[DH]` — **최소 변경**: `roadTypes`에 **"자전거도로"** 추가(아스팔트·자전거도로·보도블럭·콘크리트·비포장·기타), 속도 단위 환산 표시 확인. 나머지는 Pi가 맞추므로 앱 프로토콜 무변경. **의존: A2 ✅**
- [ ] **B5** ⭐ Pi 실측 검증 `[CW+DJ]` — 🔶 **일부 완료**: ✅ MPU-6050 감지·중력 −0.95g / ✅ 모터 회전·정지(30~100% 듀티) / ✅ Pi 저전압 없음. ⬜ 남음: **홀센서 펄스**(연결 대기) · 실제 200Hz·FIFO 오버플로 0·CSV gap. **의존: A3·A5 ✅**
- [ ] **B6a** ⭐ **B7 전 필수 코드 3종** `[CW+DJ]` — ① `DUTY_MAX=0.75` ② 소프트스타트(듀티 0→목표 0.3~0.5초 램프) ③ **스톨 감지→정지**(듀티>0인데 엔코더 무펄스 N초). **전원계산 §3의 "L298N 유지 전제"** — 없이 지면 주행하면 스톨 시 채널 2.4A로 연속정격(2A) 초과. 스톨감지 위치는 CW 제안 **controller**(`on_error→_safe_stop` 경로 재사용, 워치독과 같은 계층) — DJ 합의 필요. **의존: B6(엔코더 펄스 필요)**
- [ ] **B6** 엔코더 실측 보정 `[DJ]` — 자석 수·바퀴 둘레 실측 → config 2상수 보정. **의존: A3·A5**
- [ ] **B7** PID 실물 튜닝 `[DJ]` — 목표속도 수렴 확인(**바퀴 공중 먼저 → 지면**). **의존: A3·A5·B6·B6a(지면 주행 전 필수)** · **C1의 선행조건**(아래 참조)
- [ ] **B8** D-1 속도 정책 수치 통일 `[DJ+CW]` — TARGET_SPEED=SPEED_SAFE 하나로, 주의·위험 값 확정. **의존: B7**
- [ ] **B9** 앱↔Pi 실연동 `[DH+CW]` — 연결·명령·텔레메트리 E2E. **의존: B3·B4**
- [ ] **B10** 시연 노면 트랙 제작 `[MJ]` — 아스팔트·보도블럭·자갈 등 노면 샘플판 (2m×2m 부스 고려)
- [ ] **B11** 킥보드 외형 셸 프린팅·조립 `[MJ]` — **의존: A7**

### Phase C — 데이터·모델 (W2~W3: 8/20~8/29)

- [ ] **C1** ⭐ 본 데이터 수집 `[공동, CW 주관]` — RC카로 노면 5종 × 세션 5개+ × 10~20초, 1주행=1파일. **의존: B5(수집 검증) · A1(성립) · B6·B7(정속 확보)**
  - ⚠️ **정속(B7) 없이 수집하면 안 된다.** 진동 특징 주파수는 대략 `속도 ÷ 요철간격`이라 **속도가 2배면 특징도 2배로 이동**한다(0.5초/200Hz 윈도우에 그대로 찍힘). 결과:
    1. 같은 노면인데 스펙트럼이 제각각 → 클래스 내 분산 폭증
    2. 더 위험 — 자갈길은 느리게, 아스팔트는 빠르게 몰게 되어 **노면 라벨과 속도가 상관** → 모델이 노면이 아니라 **속도를 학습**(shortcut). 검증 정확도는 높은데 DEMO에서 무너지고 원인 추적도 어려움
  - B7 없이는 **오픈루프 duty**라 노면 저항에 따라 속도가 변한다(자갈길에서 같은 duty로도 더 느려짐) → 위 2번이 자동으로 발생
  - DEMO는 policy가 노면→속도를 바꾸는 **피드백 루프**다. 학습이 정속 기준이 아니면 루프가 진동한다
  - 프로젝트 핵심 주장이 "수집=시연 동일 RC카 → **도메인 갭 0**"인데, 수집만 속도제어가 없으면 **바로 그 갭이 생겨 논리가 자기모순**이 된다
  - 권장: 수집 시 목표속도를 고정 구간(예: 0.3 / 0.4 / 0.5 m/s)으로 나눠 **라벨에 함께 기록** → C3(윈도우 비교)에 그대로 재사용
- [ ] **C2** ⭐ 모델 학습(PC) `[CW]` — features+RandomForest 기준선 → 필요 시 1D-CNN. 세션단위 train/test·혼동행렬. **의존: C1**
- [ ] **C3** 시간 vs 거리 윈도우 비교 실험 `[CW]` — 보고서 어필 포인트. **의존: C1**
- [ ] **C4** ⭐ 모델 탑재 `[CW]` — TFLite 변환(또는 경량 대안) → `StubModel` 교체 → DEMO 모드 동작. **의존: C2·B1**

### Phase D — 검증·시연 (W3: 8/24~8/31)

- [ ] **D1** ⭐ E2E 통합 테스트 `[공동]` — 앱[START]→정속주행→수집/시연→[STOP]. COLLECT gap 없음·DEMO 반응<1초. **의존: B9·C4**
- [ ] **D2** ⭐ 안전 시나리오 테스트 `[CW+DJ]` — BLE끊김→정지 / Sampler사망→워치독 / SIGTERM→정지·파일보존 / 저신뢰→감속. **의존: B2·D1**
- [ ] **D3** ⭐ 실주행 시연 리허설 `[공동]` — 트랙에서 자갈길 감속→아스팔트 복귀, 로그+영상. **의존: D1·B10·B11**

### Phase E — 제출물 (W3~마감: 8/25~9/3)

- [ ] **E1** 팀명 확정 `[공동]` — 20Byte 이내·영문 대문자·"팀" 금지. **즉시 가능, 늦어도 8/20**
- [ ] **E2** 제출용 레포 `[CW]` — `2026ESWContest_free_<팀명>` Public 생성·코드 이관(또는 rename). **의존: E1**
- [ ] **E3** ⭐ 개발완료보고서 PPT 20p `[공동, 초안 CW]` — 필수 7항목(개요3p/환경·프로그램 10p/장애·차별성·기대효과 5p/일정·분장 2p). 소스·영상 링크 삽입. **8/25 초안 시작**
- [ ] **E4** ⭐ 시연영상 3분·720p `[MJ 촬영, 공동]` — 작품 시연+설명, YouTube 업로드, 제목 규격. **의존: D3**
- [ ] **E5** ⭐ 제출 `[CW]` — 구글폼: 참가신청서+보고서 PDF+GitHub·YouTube 링크. **9/1까지 완료 목표(마감 9/3)**

---

## 3. 주차별 캘린더

| 주차 | 기간 | 핵심 목표 | 태스크 |
| --- | --- | --- | --- |
| **W1** | 8/10~8/17 | 블로커 제거 | ~~A3 배선~~✅ ~~A4~~✅ ~~A5~~✅ ~~A6~~✅ ~~B1~~✅ ~~B2~~✅ / **남음: 섀시 조립 마무리·홀센서 연결 → A1(예비실험)·A2(#10 합의)·A7(3D)·E1(팀명)** |
| **W2** | 8/17~8/24 | 통합 완성 | B5 나머지(홀센서·200Hz) → B6(보정) → **B6a(안전 3종)** → B7(PID) → B8 / B3·B4·B9(#10 합의 후) / B10~B11 / C1 시작 |
| **W3** | 8/24~8/31 | 모델·검증·제출물 | C2~C4(모델)·D1~D3(E2E·안전·리허설)·E2~E4(레포·보고서·영상) |
| **마감주** | 8/31~9/3 | 제출 | E3·E4 마무리 → **E5 제출 (9/1 목표)** |

**크리티컬 패스**: A1 → ~~A4~~✅ → (~~A3·A5~~✅→B5) → **B6 → B6a → B7** → C1 → C2 → C4 → D1 → D3 → E4 → E5
→ 현재 선두는 **B5 나머지(홀센서 연결)** 과 **A1(예비실험 — RC카로 바로 가능)**. 하드웨어 조립이 끝나는 순간 두 개가 동시에 열린다.

**CW 코드 — B3 ✅ 끝. 다음은 B6a(안전 3종)** — **소프트스타트·DUTY_MAX·스톨감지**를 실측과 함께. 통신은 Pi 켜지면 `rfcomm_setup.sh` → `--app --real`로 bind 확인 후 B9(앱 E2E, B4 선행).
이후 CW 작업(C2·C4)은 데이터 대기 → **B5·B6a·A1이 진행축**.

## 4. 리스크 & 대비

| 리스크 | 영향 | 대비 |
| --- | --- | --- |
| A1 판정 "구분 약함" | 프로젝트 전제 흔들림 | 샘플레이트↑(500Hz)·센서위치 변경 후 재실험. **그래서 W1에 반드시** |
| 부품 배송 지연 | B5 이후 전부 밀림 | **A4(반나절) 끝내고 곧바로 A3 발주** / 그동안 CW는 A6·B1·B2 진행 |
| **전원 재계산 없이 발주** | 정격 미달 → **재발주**(국내 2~3일, 해외 1~2주) → 크리티컬 패스 통째로 밀림 | A4를 A3 앞에 고정. 계산은 반나절, 재발주는 1~2주 — **비용 비대칭** |
| **정속 미확보 상태로 C1 수집** | 모델이 노면 대신 속도를 학습 → DEMO 실패. **C1부터 재수집**(공동작업·노면 재방문) | C1 의존에 B6·B7 명시. 순서만 지키면 **일정 추가 0** |
| TFLite 변환 난항 | C4 지연 | RandomForest를 순수 파이썬 추론으로 탑재하는 폴백(속도 충분) |
| ~~#10 합의 지연~~ ✅ 종결 | — | 앱 기준 RFCOMM 통일 확정. Pi 교체 완료(B3 ✅) |
| 시간 부족 | 제출물 품질 | E3 보고서는 **8/25부터 병행 작성**(다 끝나고 쓰지 않기) |

## 5. 완료 기준 (Definition of Done)

1. 앱 START→RC카 정속주행→노면별 자동 감속→STOP이 **트랙에서 재현**된다 (영상 확보)
2. COLLECT 모드 CSV 결측 없음, 모델 세션단위 정확도 목표(≥70%) 달성
3. 안전 시나리오 전 항목 통과 (끊김·워치독·SIGTERM 모두 모터 정지)
4. 보고서 20p·영상 3분·Public 레포 — **9/1 제출 완료**
